<%= form_for(@venue) do |f| %>
  <% if @venue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue.errors.count, "error") %> prohibited this venue from being saved:</h2>

      <ul>
      <% @venue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :foursquare_id %><br />
    <%= f.text_field :foursquare_id %>
  </div>
  
  <div class="field">
    <%= f.label :unlock_points %><br />
    <%= f.text_field :locked %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
